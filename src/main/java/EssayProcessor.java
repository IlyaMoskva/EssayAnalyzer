import java.util.Map;

/**
 * Logic class to calculate words and fulfill Frequency Map
 * @Method process returns FreqMap of suitable words
 * Uses Word checker to filter out non-dictionary words.
 */
public class EssayProcessor {

    private static Map<String, Integer> process(String essay) {
        // Implement logic to process a single essay
        // This method should extract words, count their occurrences, and return the word counts
        // You can reuse the logic you'll implement in the final version here
        return null;
    }
}
